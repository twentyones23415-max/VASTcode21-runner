name: VASTcode21 marketing planner

on:
  workflow_dispatch:
  schedule:
    - cron: '25 6 * * *'

permissions:
  contents: write

concurrency:
  group: vastcode21-marketing
  cancel-in-progress: false

jobs:
  plan:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Generate safe organic content queue
        run: python tools/marketing_planner.py

      - name: Commit queue when changed
        shell: bash
        run: |
          git config user.name 'VASTcode21 Marketing Bot'
          git config user.email 'vastcode21-bot@users.noreply.github.com'
          git add marketing/queue.json
          if git diff --cached --quiet; then
            echo 'No marketing queue change.'
          else
            git commit -m 'chore: refresh marketing content queue'
            for attempt in 1 2 3; do
              git pull --rebase origin main && git push && break
              if [ "$attempt" -eq 3 ]; then
                echo 'Could not publish marketing queue after retries' >&2
                exit 1
              fi
              sleep 3
            done
          fi
