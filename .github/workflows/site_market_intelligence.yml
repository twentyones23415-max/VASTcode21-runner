name: VASTcode21 public market intelligence

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'tools/site_market_feed.py'
      - 'config/context_sources.json'
      - '.github/workflows/site_market_intelligence.yml'
  schedule:
    - cron: '23 * * * *'

permissions:
  contents: write

concurrency:
  group: vastcode21-site-market-intelligence
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Refresh public market intelligence snapshot
        run: python tools/site_market_feed.py
      - name: Commit public feed when changed
        shell: bash
        run: |
          git config user.name 'VASTcode21 Market Monitor'
          git config user.email 'vastcode21-bot@users.noreply.github.com'
          git add site/data/market_intelligence.json
          if git diff --cached --quiet; then
            echo 'No public feed change.'
          else
            git commit -m 'chore: refresh public market intelligence'
            for attempt in 1 2 3; do
              git pull --rebase origin main && git push && break
              if [ "$attempt" -eq 3 ]; then
                echo 'Could not persist public market feed' >&2
                exit 1
              fi
              sleep 3
            done
          fi
