(()=>{const panels=[...document.querySelectorAll('[data-panel]')];const controls=[...document.querySelectorAll('[data-view]')];const navButtons=[...document.querySelectorAll('.view-nav [data-view]')];function show(name){panels.forEach(p=>p.classList.toggle('active',p.dataset.panel===name));navButtons.forEach(b=>b.classList.toggle('active',b.dataset.view===name));}controls.forEach(el=>el.addEventListener('click',e=>{const name=el.dataset.view;if(!name)return;if(el.tagName==='A'&&el.getAttribute('href')==='#')e.preventDefault();show(name);}));const canvas=document.getElementById('field');if(!canvas||matchMedia('(prefers-reduced-motion: reduce)').matches)return;const ctx=canvas.getContext('2d');let w=0,h=0,dpr=1,pts=[];function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);const count=Math.max(20,Math.min(52,Math.floor(w/34)));pts=Array.from({length:count},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.12,vy:(Math.random()-.5)*.12,r:Math.random()*1.2+.35}));}function draw(){ctx.clearRect(0,0,w,h);for(const p of pts){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;ctx.beginPath();ctx.fillStyle='rgba(82,221,255,.22)';ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill();}for(let i=0;i<pts.length;i++){for(let j=i+1;j<pts.length;j++){const a=pts[i],b=pts[j],dx=a.x-b.x,dy=a.y-b.y,dist=Math.hypot(dx,dy);if(dist<115){ctx.beginPath();ctx.strokeStyle=`rgba(82,221,255,${(1-dist/115)*.055})`;ctx.lineWidth=.5;ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke();}}}requestAnimationFrame(draw);}addEventListener('resize',resize,{passive:true});resize();draw();})();